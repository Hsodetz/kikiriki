<%= form_for(materium) do |f| %>
  <% if materium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(materium.errors.count, "error") %> prohibited this materium from being saved:</h2>

      <ul>
      <% materium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nombre %>
    <%= f.text_field :nombre, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :colegio_id %>
    <%= f.select :colegio_id, options_from_collection_for_select(Colegio.all, :id, :nombre), class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :horario %>
    <%= f.text_field :horario, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :profesor_id %>
    <%= f.select :profesor_id, options_from_collection_for_select(Profesor.all, :id, :nombres), class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit 'Crear', class: "btn btn-warning btn-sm" %>
  </div>
<% end %>
